fetched_at_utc=2026-07-26T16:00:00.000000+00:00
source=hand-curated (no automated index/membership source exists for crypto the way
  Wikipedia's S&P 500 / Nasdaq-100 pages do) — top 15 USD pairs by market
  prominence/liquidity that are BOTH tradable on the two crypto brokers already
  linked in this project (Coinbase, Kraken) AND verified to return real minute-bar
  aggregates from Polygon under the X:<SYMBOL>USD ticker format. Every ticker in
  this file was individually verified against a live Polygon API call before being
  added (2026-07-26) - see CLAUDE_NOTES.txt "Phase 1 (crypto)" for the verification
  method. DOGE is included for trading volume/liquidity, not fundamentals - same
  reasoning a liquid, actively-traded name earns a slot in the equities universes.
  Stablecoins (USDC, USDT, etc.) are deliberately excluded - a strategy engineered
  to trade price MOVEMENT has nothing to work with on an asset pegged to $1.
count=15
note=this is a periodic snapshot of a hand-picked list, not a live feed - re-review
  the list occasionally (market-cap rankings shift) rather than treating it as
  permanent. No refresh script exists for this one (nothing to scrape); editing the
  CSV directly is the "refresh" for this universe.
note2=SYMBOL MAPPING — RESOLVED 2026-08-19. These tickers are in POLYGON's data
  format (X:BTCUSD). Coinbase's execution API uses product-id format (BTC-USD) -
  brokers/coinbase.py's _product_id() already handled this via strip+rejoin, no
  changes needed. Kraken uses its own altname pair codes - brokers/kraken.py's
  new _pair() does the same strip+rejoin, with two confirmed exceptions (Kraken
  uses "XBT" for Bitcoin and "XDG" for Dogecoin, not "BTC"/"DOGE" - "DOGEUSD" IS
  a real Kraken pair but a DIFFERENT, unrelated one). All 15 tickers' mappings for
  both brokers were verified against each broker's own live API before trusting
  this note (Kraken's public AssetPairs endpoint; Coinbase's mapping was already
  live-tested via its own account calls). Both submit_market_order() paths now
  accept either ticker format directly - no separate mapping layer needed at the
  call site.
